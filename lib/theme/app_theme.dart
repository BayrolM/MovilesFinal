import 'package:flutter/material.dart';

class AppColors {
  //Rosa pastel principal
  static const Color pinkPrimary = Color(0xFFF7C6D9);

  // Rosa pastel secundario
  static const Color pinkLight = Color(0xFFFBE0EB);

  // Rosa pastel más oscuro para contraste
  static const Color pinkDark = Color(0xFFE8A5BE);

  // Blanco
  static const Color white = Color(0xFFFFFFFF);

  // Negro suave para UI
  static const Color black = Color(0xFF1A1A1A);

  // Neutros para fondo y textos
  static const Color greyLight = Color(0xFFF5F5F5);
  static const Color grey = Color(0xFFBEBEBE);
  static const Color greyDark = Color(0xFF4A4A4A);
}
